# AskData Studio 作品集演示脚本

## 演示准备

- 使用 `sales` 账号创建以下对话，确保侧栏中的案例集中在同一个账号下。
- 每个主题使用“新建对话”单独创建，避免不同能力混在同一条会话中。
- 查询完成后保留 SQL、结果表和结果说明，不要清空对话。
- 安全拦截与权限隔离通过“评测中心”展示，不在演示历史中保留失败会话。

## 案例一：复杂多表问数

提问：

```text
按订单地区统计各商品分类的销量
```

预期展示：

- 自动检索订单、订单明细、商品、商品分类等相关 Schema。
- 使用合法外键完成多表关联。
- 返回按地区与商品分类聚合的销量。
- 可展开查看 SQL，并结合“本次查询口径”解释指标、维度与关联字段。

讲解重点：用户只提供业务问题，系统完成字段级检索、关联路径选择、SQL 生成、执行和结果说明。

## 案例二：模糊口径澄清

首次提问：

```text
按地区看平均单价
```

在澄清选项中选择：

```text
订单成交均价 / 按订单大区
```

预期展示：

- 系统识别“价格”和“地区”存在多个业务口径，不直接猜测。
- 用户确认后在同一对话中恢复任务并完成查询。
- 最终查询使用订单大区与订单明细成交单价。

讲解重点：AskData 在高歧义问题上优先澄清，降低“SQL 能执行但业务口径错误”的风险。

## 案例三：右侧上下文——指定查询字段

1. 新建对话，打开右侧“本次上下文”。
2. 在“字段与结果”中搜索并拖入“查询字段”：
   - `大区`（平台店铺，`stores.region`）
   - `销售价`（SKU销售单元，`product_skus.sale_price`）
3. 提问：

```text
按店铺所属大区统计SKU销售价的平均值
```

预期展示：

- 右侧显示 2 个用户确认字段。
- Schema 检索将它们标记为用户确认来源并纳入查询。
- 查询沿 `product_skus → products → stores` 关联，不混用订单地区或成交单价。

讲解重点：右侧上下文允许业务人员显式约束字段口径，同时仍由系统补齐必要关联字段。

## 案例四：真实业务语义与交付

提问：

```text
帮我找出目前还没有配置专属封面的歌曲
```

预期展示：

- 返回仍使用默认封面 `public_third_part.jpeg` 的 8 条歌曲资源。
- 结果包含资源 ID、歌曲名称、歌手、专辑、分类和当前封面。
- 使用“导出 Excel”展示结果交付能力。

讲解重点：“未配置专属封面”是业务定义，不等同于数据库空值，体现语义层对真实业务口径的约束。

## 案例五：时间查询并保存结果

提问：

```text
按下单时间统计2026年7月各地区的订单量
```

查询完成后点击结果卡右上角的“保存”。

预期展示：

- 使用左闭右开的月份边界过滤 2026 年 7 月。
- 按地区返回订单量并生成与结果一致的说明。
- 保存后，该结果出现在右侧“字段与结果”的结果表区域。

讲解重点：展示时间口径、结果说明可信度，以及可复用的数据记忆。

## 案例六：右侧上下文——引用历史结果

1. 新建对话并打开右侧“本次上下文”。
2. 将案例五保存的结果拖入“参考结果”。
3. 提问：

```text
基于右侧参考结果，指出订单量最高和最低的地区，并计算两者相差多少单。只使用参考结果，不重新查询数据库。
```

预期展示：

- 右侧显示 1 张参考结果表。
- 返回综合分析，不重新生成数据库查询 SQL。
- 结果中标识综合分析来源，结论与参考表中的数值一致。

讲解重点：查询字段用于约束 Text-to-SQL，参考结果用于二次分析；两种上下文用途明确分离。

## 推荐讲解顺序

1. 复杂多表问数：证明基础能力。
2. 模糊口径澄清：证明业务可靠性。
3. 指定查询字段：展示右侧查询上下文。
4. 真实业务语义与 Excel：展示落地价值。
5. 保存结果与引用分析：展示数据记忆闭环。
6. 评测中心：以三轮 `30/30`、SQL 执行 `24/24` 收尾，并补充只读安全和账号权限隔离。
